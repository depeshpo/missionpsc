import type { ProgressKind } from "@/lib/hooks/useUserProgress";

/** Progress kind for syllabus unit completion (the DB `user_progress.kind`). */
export const SYLLABUS_PROGRESS_KEY: ProgressKind = "unit_complete";
