import { ComingSoon } from "@/components/layout/ComingSoon";

export default function SyllabusPage() {
  return (
    <ComingSoon
      title="Syllabus Map"
      description="The full Stage → Paper → Section → Unit map with marks weights and coverage tracking."
      note="This is the first feature we'll build out. The syllabus data is already encoded in src/data/syllabus.ts."
    />
  );
}
