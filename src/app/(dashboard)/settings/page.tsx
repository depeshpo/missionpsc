import { ComingSoon } from "@/components/layout/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Theme, progress reset, and profile (authentication arrives in a later version)."
    />
  );
}
